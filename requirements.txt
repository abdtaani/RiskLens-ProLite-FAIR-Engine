# requirements.txt
# قائمة بالمكتبات المطلوبة للمشروع.

# لـ Pandas ومعالجة البيانات
pandas==2.2.2

# لواجهة سطر الأوامر
typer==0.12.3
typing-extensions==4.12.0 # مطلوب بواسطة Typer

# لتوليد تقارير PDF
reportlab==4.2.0

# لتوليد الرسوم البيانية في تقارير PDF
matplotlib==3.9.0
seaborn==0.13.2

# لتحليل ملفات Windows .evtx (اختياري، يرجى ملاحظة التحذير في log_reader.py)
# python-evtx==0.6.1 # قد يتطلب تثبيت بعض تبعيات C++
